import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
  input: {
    width: "100%",
    height: 52,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: "#121214",
    paddingLeft: 5
  },
});
